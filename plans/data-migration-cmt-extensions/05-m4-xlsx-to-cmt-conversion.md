# Milestone 4 — Staging Excel → CMT Package Conversion

## Goal

Convert a populated staging Excel workbook into a complete CMT-compatible data package (data.xml + data_schema.xml + sidecars + manifest), ready for import via `txc data package import`.

## CLI Command

```
txc data package convert --input migration.xlsx --schema data_schema.xml -o ./package/ [--keys data_keys.xml] [--on-error warn|fail]
```

### Arguments

| Argument      | Description                                                         |
|---------------|---------------------------------------------------------------------|
| `--input`     | Path to the populated staging Excel workbook                        |
| `--schema`    | Path to `data_schema.xml` (generated by M1)                        |
| `-o`          | Output directory for the package                                    |
| `--keys`      | Optional path to `data_keys.xml` for alternate key resolution       |
| `--on-error`  | Error handling mode: `warn` (log and continue) or `fail` (abort)    |

## Output

A complete package directory:

```
package/
├── data.xml
├── data_schema.xml
├── [Content_Types].xml
├── txc-package.xml
├── data_keys.xml          (if alternate keys specified)
├── data_owners.xml        (if ownerid columns present)
├── data_state.xml         (if statecode columns present)
├── data_bpf.xml           (if BPF Advancement sheet present)
├── data_actions.xml       (if API sheets present)
├── data_callerid.xml      (if caller ID data present)
└── data_postimport.xml    (execution order manifest)
```

## Conversion Pipeline

The conversion follows a strict sequential pipeline:

### 1. Read

- Open the workbook with `StagingExcelReader` (from M2c).
- Parse all entity sheets (migrated only — `include_in_cmt = true`).
- Parse optionset sheets, M:N sheets, BPF sheet, API sheets.

### 2. Validate

- Verify all required columns present per entity.
- Check data types match expected schema types.
- Flag empty required fields.
- **Detect source duplicates**: check for duplicate primary keys (column A) and duplicate alternate keys (if `data_keys.xml` provided) within each entity sheet. Options when found: `error` (default), `first-wins`, `last-wins`, `warn`.
- **Per-field lookup fallback**: each lookup column can have its own failure strategy specified in schema or metadata: `error` (fail row), `nullify` (set to null), `default:<guid>` (fallback value). Global `--on-error` applies to non-lookup validation failures.
- Collect validation issues (warnings or errors based on `--on-error`).
- **Structured error report**: emit per-row error details including original row data, row number, field name, and error message. Not just counts — the actual failing data for debugging.

### 3. Resolve Optionset Composites

Convert composite "Label [value]" back to integer values:

```
"Active [1]"  →  1
"Won [3]"     →  3
```

Parse the value from between the square brackets. Validate that the extracted value exists in the schema's optionset definition.

### 4. Resolve Lookup Composites

Convert composite "Name [GUID]" back to GUID entity references:

```
"Contoso Ltd [a1b2c3d4-...]"  →  a1b2c3d4-...
```

Parse the GUID from between the square brackets. Validate that the GUID format is correct.

### 5. Synthesize Deterministic GUIDs

For new records that don't have a GUID in column A, generate a **deterministic GUID** using the alternate key values (from `data_keys.xml`). This ensures:

- The same record always gets the same GUID across repeated conversions.
- Cross-references between entities resolve correctly.
- Re-imports are idempotent (same GUID = upsert, not duplicate).

Algorithm: MD5 hash of `entityname|keyfield1=value1|keyfield2=value2` — matching the existing `txc` HTTP endpoint for key resolution.

### 6. Emit `data.xml`

Write the CMT-compatible data XML with all records, using the resolved values (integers for optionsets, GUIDs for lookups).

### 7. Emit Sidecars

Generate sidecar files from the Excel data:

| Source                     | Sidecar                |
|----------------------------|------------------------|
| `ownerid` columns          | `data_owners.xml`      |
| `statecode` columns        | `data_state.xml`       |
| BPF Advancement sheet      | `data_bpf.xml`         |
| API sheets                 | `data_actions.xml`     |
| Caller ID data             | `data_callerid.xml`    |

### 8. Emit Manifest

Write `txc-package.xml` listing all generated sidecars and the execution order in `data_postimport.xml`.

### 9. Validation Report

Output a summary of:

- Records processed per entity
- Row diff classification: added, modified, deleted, unchanged, and invalid
- Warnings (type coercion issues, missing optional fields)
- Errors (if `--on-error fail`, these would have aborted earlier)
- Checksum mismatches (see below)

## Type Coercion Table

| Schema type | Cell raw | Emitted XML `value` | Notes |
|---|---|---|---|
| `string` | `"abc"` | `abc` | Trim trailing whitespace optional. |
| `int` | `42` / `42.0` | `42` | Reject `4.5`. |
| `decimal`/`money`/`float` | `1.23` | `1.23` | Use invariant culture. |
| `bool` | `Yes`/`No`/`true`/`false`/`1`/`0` | `true`/`false` | Case-insensitive. |
| `datetime` | Excel date / ISO string | ISO 8601 UTC | Honor schema `dateMode`. |
| `picklist` / `state` / `status` | Composite "Label [value]" or numeric | numeric value | Resolve via optionset sheet. |
| `multiselectpicklist` | `"label1, label2"` | `value1;value2` | Sentinel `-1` is forbidden — error. |
| `entityreference` / `customer` / `owner` | Composite "Name [GUID]" | GUID | Alt-key references rewritten via M5 `KeyAwareXmlRewriter` when `--keys` is provided. |
| `guid` | text | text (validated) | Regex `^[0-9a-f-]{36}$`. |

## Checksum Verification

Row checksums (column B in the Excel) are **recomputed** during conversion and compared against the stored values:

- **Match**: Row is clean — no unexpected modifications.
- **Mismatch**: Row was modified outside of normal editing flow. A warning is emitted (or error if `--on-error fail`).

This enables delta detection: if checksums match, the row hasn't changed since the template was generated or last converted.

### Diff Semantics

For workbooks produced by `generate-xlsx` with preloaded data or by `export-xlsx`, the `_meta` sheet stores a visible row catalog containing `(entity, id, checksum)` from the source package/workbook generation time. During conversion:

| Classification | Rule | Effect |
|---|---|---|
| `unchanged` | Row id exists in the catalog and checksum matches | Included or skipped according to command mode; reported as unchanged |
| `modified` | Row id exists in the catalog and checksum differs | Included in `data.xml`; warning/error depends on checksum policy |
| `added` | Row id is absent from the catalog | Treated as a new record; deterministic GUID synthesis may fill the id |
| `deleted` | Catalog row id is missing from the entity sheet | Reported in the diff report; Phase 1 does not delete target records automatically |
| `invalid` | Row fails type/reference/schema validation | Excluded only if warning mode allows continuing; otherwise conversion fails |

Checksum mismatch alone means "row changed", not "conflict with target". Target-side conflict detection belongs to pre-import validation / future load-journal work.

## Replaces Existing Command

This replaces the existing naive `DataPackageConvertCliCommand`. The old `--no-schema` flag is **deprecated for one release cycle** (emits a deprecation warning, then removed in the following release).

## Implementation

New type: `XlsxToCmtConverter` in `TALXIS.CLI.Features.Data/Xlsx/`

The converter orchestrates the full pipeline, delegating to:

- `StagingExcelReader` (M2c) for reading
- `PackageManifest` + sidecar writers (M3) for output
- `IRecordKeyService` for deterministic GUID synthesis
- `SchemaXmlWriter` (M1) for schema copy

## Tests

| Test                              | Description                                                          |
|-----------------------------------|----------------------------------------------------------------------|
| Full round-trip                   | Generate template → fill data → convert → parse data.xml → assert    |
| Optionset resolution              | Composite "Label [value]" → correct integer in data.xml              |
| Lookup resolution                 | Composite "Name [GUID]" → correct GUID in data.xml                  |
| Bool coercion                     | "Yes"/"No" → true/false                                             |
| Date coercion                     | Excel date → ISO 8601 UTC                                           |
| Deterministic GUIDs               | Same alternate key values → same GUID on repeated runs              |
| Checksum mismatch warning         | Modified row → warning in report                                    |
| Missing required field            | Empty required field → error (--on-error fail) or warning            |
| Sidecar generation                | ownerid present → data_owners.xml generated                         |
| Empty package                     | No data rows → valid but empty package                              |
| Determinism                       | Same input → identical output on repeated runs                      |
| Failure modes                     | Invalid Excel structure → clear error message                       |

## Done When

- `txc data package convert` produces a complete package directory
- The generated `data.xml` is accepted by `txc data package import`
- All sidecars are correctly generated from Excel data
- Checksum verification works correctly
- All tests pass
